#include "Event.h"
using namespace std;

int Event::readEvtFile(State& currentState, int turn)
{
	if (turn >= 0 && turn <= 4)
	{
		if ()
	}
}

void Event::makeEvt(State& currentState, int turn, bool& alive)
{

}